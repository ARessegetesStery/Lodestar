---
name: lodestar-design
description: Use when the intent is settled but the design is not; investigate the codebase, return a concrete design, and write it to a spec.
---

# Lodestar: Design

This is the Codex entry point for Lodestar's canonical design skill. Before acting, read and follow `../../../skills/design/SKILL.md` in full. That file is authoritative.

## Codex compatibility

The Lodestar repository root is three directories above this file. When the canonical skill refers to `/lodestar:<skill>`, use the matching Codex skill name: `$lodestar-<skill>`. When it refers to `${CLAUDE_PLUGIN_ROOT}`, substitute that repository root. Use Codex-native tools while preserving the canonical skill's constraints and outcomes.
